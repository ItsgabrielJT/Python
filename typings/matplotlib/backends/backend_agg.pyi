"""
This type stub file was generated by pyright.
"""

from matplotlib import _api
from matplotlib.backend_bases import FigureCanvasBase, FigureManagerBase, RendererBase, _Backend, _check_savefig_extra_args

"""
An `Anti-Grain Geometry`_ (AGG) backend.

Features that are implemented:

* capstyles and join styles
* dashes
* linewidth
* lines, rectangles, ellipses
* clipping to a rectangle
* output to RGBA and Pillow-supported image formats
* alpha blending
* DPI scaling properly - everything scales properly (dashes, linewidths, etc)
* draw polygon
* freetype2 w/ ft2font

Still TODO:

* integrate screen dpi w/ ppi and text

.. _Anti-Grain Geometry: http://agg.sourceforge.net/antigrain.com
"""
backend_version = ...
def get_hinting_flag():
    ...

class RendererAgg(RendererBase):
    """
    The renderer handles all the drawing primitives using a graphics
    context instance that controls the colors/styles
    """
    lock = ...
    def __init__(self, width, height, dpi) -> None:
        ...
    
    def __getstate__(self): # -> dict[str, Unknown]:
        ...
    
    def __setstate__(self, state): # -> None:
        ...
    
    @_api.deprecated("3.4")
    def get_content_extents(self): # -> tuple[Any, Any, Any, Any]:
        ...
    
    @_api.deprecated("3.4")
    def tostring_rgba_minimized(self): # -> tuple[NDArray[Unknown], tuple[Any, Any, Any, Any]]:
        ...
    
    def draw_path(self, gc, path, transform, rgbFace=...):
        ...
    
    def draw_mathtext(self, gc, x, y, s, prop, angle): # -> None:
        """Draw mathtext using :mod:`matplotlib.mathtext`."""
        ...
    
    def draw_text(self, gc, x, y, s, prop, angle, ismath=..., mtext=...): # -> None:
        ...
    
    def get_text_width_height_descent(self, s, prop, ismath): # -> tuple[Unknown | Literal[0], Unknown | Literal[0], Unknown | Literal[0]] | tuple[Unknown, Unknown, Unknown]:
        ...
    
    def draw_tex(self, gc, x, y, s, prop, angle, *, mtext=...): # -> None:
        ...
    
    def get_canvas_width_height(self): # -> tuple[Unknown, Unknown]:
        ...
    
    def points_to_pixels(self, points):
        ...
    
    def buffer_rgba(self): # -> memoryview:
        ...
    
    def tostring_argb(self): # -> bytes:
        ...
    
    def tostring_rgb(self): # -> bytes:
        ...
    
    def clear(self): # -> None:
        ...
    
    def option_image_nocomposite(self): # -> Literal[True]:
        ...
    
    def option_scale_image(self): # -> Literal[False]:
        ...
    
    def restore_region(self, region, bbox=..., xy=...): # -> None:
        """
        Restore the saved region. If bbox (instance of BboxBase, or
        its extents) is given, only the region specified by the bbox
        will be restored. *xy* (a pair of floats) optionally
        specifies the new position (the LLC of the original region,
        not the LLC of the bbox) where the region will be restored.

        >>> region = renderer.copy_from_bbox()
        >>> x1, y1, x2, y2 = region.get_extents()
        >>> renderer.restore_region(region, bbox=(x1+dx, y1, x2, y2),
        ...                         xy=(x1-dx, y1))

        """
        ...
    
    def start_filter(self): # -> None:
        """
        Start filtering. It simply create a new canvas (the old one is saved).
        """
        ...
    
    def stop_filter(self, post_processing): # -> None:
        """
        Save the plot in the current canvas as a image and apply
        the *post_processing* function.

           def post_processing(image, dpi):
             # ny, nx, depth = image.shape
             # image (numpy array) has RGBA channels and has a depth of 4.
             ...
             # create a new_image (numpy array of 4 channels, size can be
             # different). The resulting image may have offsets from
             # lower-left corner of the original image
             return new_image, offset_x, offset_y

        The saved renderer is restored and the returned image from
        post_processing is plotted (using draw_image) on it.
        """
        ...
    


class FigureCanvasAgg(FigureCanvasBase):
    def copy_from_bbox(self, bbox):
        ...
    
    def restore_region(self, region, bbox=..., xy=...): # -> None:
        ...
    
    def draw(self): # -> None:
        ...
    
    def get_renderer(self, cleared=...): # -> RendererAgg:
        ...
    
    def tostring_rgb(self): # -> bytes:
        """
        Get the image as RGB `bytes`.

        `draw` must be called at least once before this function will work and
        to update the renderer for any subsequent changes to the Figure.
        """
        ...
    
    def tostring_argb(self): # -> bytes:
        """
        Get the image as ARGB `bytes`.

        `draw` must be called at least once before this function will work and
        to update the renderer for any subsequent changes to the Figure.
        """
        ...
    
    def buffer_rgba(self): # -> memoryview:
        """
        Get the image as a `memoryview` to the renderer's buffer.

        `draw` must be called at least once before this function will work and
        to update the renderer for any subsequent changes to the Figure.
        """
        ...
    
    @_check_savefig_extra_args
    @_api.delete_parameter("3.5", "args")
    def print_raw(self, filename_or_obj, *args): # -> None:
        ...
    
    print_rgba = ...
    @_check_savefig_extra_args
    @_api.delete_parameter("3.5", "args")
    def print_png(self, filename_or_obj, *args, metadata=..., pil_kwargs=...): # -> None:
        """
        Write the figure to a PNG file.

        Parameters
        ----------
        filename_or_obj : str or path-like or file-like
            The file to write to.

        metadata : dict, optional
            Metadata in the PNG file as key-value pairs of bytes or latin-1
            encodable strings.
            According to the PNG specification, keys must be shorter than 79
            chars.

            The `PNG specification`_ defines some common keywords that may be
            used as appropriate:

            - Title: Short (one line) title or caption for image.
            - Author: Name of image's creator.
            - Description: Description of image (possibly long).
            - Copyright: Copyright notice.
            - Creation Time: Time of original image creation
              (usually RFC 1123 format).
            - Software: Software used to create the image.
            - Disclaimer: Legal disclaimer.
            - Warning: Warning of nature of content.
            - Source: Device used to create the image.
            - Comment: Miscellaneous comment;
              conversion from other image format.

            Other keywords may be invented for other purposes.

            If 'Software' is not given, an autogenerated value for Matplotlib
            will be used.  This can be removed by setting it to *None*.

            For more details see the `PNG specification`_.

            .. _PNG specification: \
                https://www.w3.org/TR/2003/REC-PNG-20031110/#11keywords

        pil_kwargs : dict, optional
            Keyword arguments passed to `PIL.Image.Image.save`.

            If the 'pnginfo' key is present, it completely overrides
            *metadata*, including the default 'Software' key.
        """
        ...
    
    def print_to_buffer(self): # -> tuple[bytes, tuple[int, int]]:
        ...
    
    @_check_savefig_extra_args()
    @_api.delete_parameter("3.5", "args")
    def print_jpg(self, filename_or_obj, *args, pil_kwargs=..., **kwargs): # -> None:
        """
        Write the figure to a JPEG file.

        Parameters
        ----------
        filename_or_obj : str or path-like or file-like
            The file to write to.

        Other Parameters
        ----------------
        pil_kwargs : dict, optional
            Additional keyword arguments that are passed to
            `PIL.Image.Image.save` when saving the figure.
        """
        ...
    
    print_jpeg = ...
    @_check_savefig_extra_args
    def print_tif(self, filename_or_obj, *, pil_kwargs=...): # -> None:
        ...
    
    print_tiff = ...


@_Backend.export
class _BackendAgg(_Backend):
    FigureCanvas = FigureCanvasAgg
    FigureManager = FigureManagerBase


